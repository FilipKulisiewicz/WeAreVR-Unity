using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using RosMessageTypes.Geometry;
using Unity.Robotics.ROSTCPConnector.ROSGeometry;
using System;

// reference only
public class SphereRotUnwinding : MonoBehaviour
{
    public GameObject sphere;  
    //public static event Func<float, float, bool, PositionRotation> TransformRequest;
    //public static event Func<float, bool, PositionRotation> TransformRequest;
    public static event Action updateFrame;
    public float delay = 0.12f; 
    public float frametime = 1.0f / 29.97f; //correction needed
    private bool interpolate = true;
    
    private void OnEnable(){
        WebCamDetect.newFrame += Unwind;
    }
    private void OnDisable(){
        WebCamDetect.newFrame -= Unwind;
    }

    public void Unwind(){
        //PositionRotation fromBuffer = (PositionRotation)TransformRequest?.Invoke(frametime, delay, interpolate);
        //sphere.transform.rotation = fromBuffer.rotation;
    }
}

